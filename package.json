{
  "name": "flygplot",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "cd frontend && npm run dev",
    "build": "cd frontend && npm run build",
    "build:matrix": "node scripts/build-onoff-matrix.mjs",
    "prepush": "npm run build"
  },
  "dependencies": {
    "better-sqlite3": "^12.11.1",
    "cors": "^2.8.6",
    "dotenv": "^16.4.7",
    "express": "^5.1.0"
  }
}
